# brain
brain C++ simulator

## fork and test your hardware is intellected
your git should already be configured
and available in the console.
default it will do every midnight
```git commit``` and ```git pull``` request
of best found self version sources,
but you always can change it time in ```main.cpp```
see cron-like string parameter of sheduler

## run
```./run.sh```
it will build and run executable
